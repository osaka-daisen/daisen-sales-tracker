# 設定手順（初回だけ・15分ほど）

木村さんが一度やれば、営業のみなさんはURLを開くだけで使えます。

---

## 1. Webツール用のスプレッドシートを作る

いまの `（新）大泉衛生営業進捗管理.xlsx` は Excel形式のため、Webツールから書き込めません。
**中身をそのままGoogleスプレッドシート形式にコピー**します。元のExcelファイルは残ります。

1. Driveで `（新）大泉衛生営業進捗管理.xlsx` を開く
2. メニューの **ファイル → Google スプレッドシートとして保存**
3. `（新）大泉衛生営業進捗管理のGoogle スプレッドシート` という新しいファイルができる
   （中身・列・これまでのデータはそのまま入っています）
4. 名前を **`大泉衛生 営業進捗（Webツール用）`** に変えておくと分かりやすいです
5. そのファイルのURLから **ID** を控える

```
https://docs.google.com/spreadsheets/d/【この部分がID】/edit
```

---

## 2. Apps Script を用意する

1. 手順1で作ったスプレッドシートを開く
2. メニューの **拡張機能 → Apps Script**
3. 出てきたエディタの中身をすべて消して、`gas/コード.gs` の中身を貼り付ける
4. 保存（フロッピーのアイコン）

   ※ SHEET_ID は設定済みです（（新）大泉衛生営業進捗管理）。書き換えは不要です。

### 動作確認
関数の選択欄で `テスト_読み取り` を選んで **実行**。
初回は「承認が必要です」と出るので、自分のGoogleアカウントで許可してください。
（「このアプリは確認されていません」と出たら **詳細 → 安全ではないページに移動** で進みます。自分が書いたスクリプトなので問題ありません）

実行ログに `件数: ○○` と出れば成功です。

---

## 3. ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車マーク → **ウェブアプリ** を選ぶ
3. 設定
   - 説明：`営業進捗ツール`
   - **次のユーザーとして実行：自分**
   - **アクセスできるユーザー：全員**
4. **デプロイ** を押す
5. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/..../exec`）をコピー

> 「アクセスできるユーザー：全員」は、営業のみなさんがGoogleログインなしで使えるようにするためです。
> URLを知らない人はアクセスできません。

---

## 4. Webツールにつなぐ

1. スマホかPCで https://osaka-daisen.github.io/daisen-sales-tracker/ を開く
2. 「最初の設定」の欄に、手順3でコピーしたURLを貼って **保存して読み込む**
3. これまでのデータが一覧に出てくれば完成です

この設定は端末ごとに1回だけです。木村さんからURLを教えていただければ、
こちらでツール本体に埋め込んでおくこともできます（そうすると営業のみなさんは何も設定せずに使えます）。

---

## 5. 営業のみなさんへの案内

> **営業進捗ツールを作りました**
> https://osaka-daisen.github.io/daisen-sales-tracker/
>
> 訪問した先をその場でスマホから登録できます。右上の「担当」に自分の名前を一度入れてください。
> エリア（区）を選んでから、その先の住所だけ入れてください。区は書かなくて大丈夫です。
> あとはステータスを選べば登録完了。金額やメモは後から足せます。
> 「案件・更新」タブで自分の案件を開くと、契約が決まったときのステータス変更や金額入力ができます。
> 「集計」タブでは、上に今日のAP件数、下に今月の活動と成績（契約件数・実績金額・達成率）が出ます。
> 成績は契約した日で数えるので、今月取った契約は今月の数字に乗ります。
> スマホのホーム画面に追加しておくと、アプリのように開けます。

---

## よくある質問

**Q. これまでのスプレッドシートはどうなりますか**
A. 元のExcelファイルはそのまま残ります。Webツールが読み書きするのは手順1で作った新しいGoogleスプレッドシートです。

**Q. スプレッドシートから直接入力してもいいですか**
A. 大丈夫です。Webツールは開くたびに最新を読み込みます。列の順番を変えず、見出しの文字（`見込み日` など）はそのままにしてください。

## 集計の考え方（2026/08 決定）

営業成績と活動量は、別々の日付で数えます。

| 指標 | 数える日 | 意味 |
|---|---|---|
| AP件数 | AP日（見込み日） | その日・その月に何件動いたか |
| 見積提出 | 見積提出日 | 商談がどれだけ前に進んだか |
| 契約件数・実績金額・達成率 | **契約日** | 成績判定。月間目標に対する実績 |

1月にAPして8月に契約した案件は、**8月の実績**になります（1月のAP件数には残ります）。

日付はステータスを変えたときに自動で入ります。営業のみなさんが日付を入力する必要はありません。

- ステータスを「見積・交渉中」「契約予定」「契約済」にした日 → 見積提出日
- ステータスを「契約済」にした日 → 契約日
- ステータスを「クローズ」にした日 → クローズ日

あとから直したいときは、案件を開いて日付欄を書き換えてください。
過去の行には契約日が入っていないので、その場合は見込み日で代用して集計します。

---

**Q. ステータスの「契約予定」はどう数えられますか**
A. 見積提出には含み、契約数と実績金額には含みません（まだ売上ではないため）。集計画面には別に「契約予定」件数と「予定金額」を出しているので、今月これから決まりそうな金額が分かります。予定金額は契約金額が空なら見積金額を使います。

**Q. 集計が合わないときは**
A. Webツールの集計は数式ではなくその場で計算しているので、月の指定ミス（全角数字など）では狂いません。数字が合わないときは、対象の月と担当の選択を確認してください。

**Q. 電波が悪い現場では**
A. 送信できなかった分は端末に保存され、画面上部に「未送信が○件あります」と出ます。電波の良い場所で「最新に更新」を押すと送信されます。
